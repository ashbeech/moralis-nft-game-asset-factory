// description for NFT in metadata file
const description = "Asset Factory";
// base url in case no unique metadata file i.e IPFS
const baseImageUri = "ipfs://";
// id for edition to start from
const startEditionFrom = 1;

module.exports = {
  description,
  baseImageUri,
  startEditionFrom,
};
